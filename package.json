{
  "name": "shivswacar-backend-root",
  "version": "1.0.0",
  "description": "Root package.json to deploy backend on Railway",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}